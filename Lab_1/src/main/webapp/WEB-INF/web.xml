<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">
    
    
    
    <welcome-file-list>
        <welcome-file>BT.html</welcome-file>
    </welcome-file-list>


    <!-- Khai báo context-param (toàn cục) -->
    <context-param>
        <param-name>appName</param-name>
        <param-value>My First Servlet App</param-value>
    </context-param>

    <!-- Khai báo servlet Bai4 -->
    <servlet>
        <servlet-name>bai4</servlet-name>
        <servlet-class>iuh.fit.se.lab_1.Bai4</servlet-class>

        <!-- init-param (riêng cho Bai4) -->
        <init-param>
            <param-name>username</param-name>
            <param-value>admin</param-value>
        </init-param>
        <init-param>
            <param-name>email</param-name>
            <param-value>admin@example.com</param-value>
        </init-param>
    </servlet>

    <!-- Mapping servlet -->
    <servlet-mapping>
        <servlet-name>bai4</servlet-name>
        <url-pattern>/bai4</url-pattern>
    </servlet-mapping>

    
</web-app>

